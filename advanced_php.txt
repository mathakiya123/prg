
 Module 2 – Advanced PHP Excercises

OOPs Concepts
THEORY  EXERCISE
---------------------------------------
1)Define Object-Oriented Programming (OOP) and its four main principles: Encapsulation, Inheritance,Polymorphism,and Abstraction.

1)Encapsulation
=> wrapping up of data or binding of  data
ex:capsule

2)Inheritance
=>Inheritance in PHP is a concept in Object-Oriented Programming (OOP) that allows one class (called the child class or subclass) to inherit properties and methods from another class (called the parent class or base class).



3)Polymorphism
=>Polymorphism in PHP is an Object-Oriented Programming (OOP) concept that allows different classes to define methods with the same name, but with different implementations

4)Abstaction 
=>An abstract class in PHP is a special kind of class that cannot be instantiated directly (you can’t create an object from it).
It is used as a base class that provides a common structure for its child classes



Practical Exercise :
----------------------------------------------------
1)Create a simple class in PHP that demonstrates encapsulation by using private and public  properties and methods.
=>
<?php
class BankAccount {
    private $balance;

   
    public function __construct($amount) {
        $this->balance = $amount;
    }

    
    public function deposit($amount) {
        $this->balance += $amount;
    }

    public function withdraw($amount) {
        if ($amount <= $this->balance) {
            $this->balance -= $amount;
        } else {
            echo "Insufficient balance!<br>";
        }
    }


    public function getBalance() {
        return $this->balance;
    }
}


$account = new BankAccount(1000);
$account->deposit(500);
$account->withdraw(200);

echo "Current Balance: " . $account->getBalance();  
?>


 1) Class

THEORY EXERCISE

1)  Explain the structure of a class in PHP,including properties and methods.
->
Class defination

A class in PHP is a template or blueprint used to create objects.
It defines properties (data/variables) and methods (functions/behaviors) that describe how an object will look and behave.


Class Declaration

A class is defined using the keyword class followed by the class name.

Example:

class Student {

 }


Properties

These are variables declared inside a class.

They hold data or information related to the object.

Properties are usually defined with access modifiers like public, private, or protected.

Example:

public $name;
private $age;


Methods

These are functions declared inside a class.

They define the actions or behavior of the object.

Methods can also have access modifiers (public, private, protected).

Example:

public function display() {
    echo "Hello, I am a student.";



*** Practical Exercise:
<?php

class car
{
    public $make;
    public $model;
    public $year;
    function __construct($make,$model,$year)
    {
            $this->make =$make;
            $this->model =$model;
            $this->year =$year;
    }
    function display()
    {
         echo "Car Make :". $this->make."<br>";
           echo" Car Model :". $this->model."<br>";
             echo"Car Year :". $this->year."<br>";
    }
 }
$ob = new car('toyota','camry',2020);
$ob->display();



?>




***) object

thery)  What is an object in OOP ? Discuss how objects are instantiated from classes in PHP.
->
is an Object in OOP?

In Object-Oriented Programming (OOP), an object is a real-world entity or an instance of a class.
It represents a specific example of a class that contains data (properties) and behaviors (methods) defined by that class.

An object allows you to use the features defined in the class — for example, storing data and calling methods

Discuss how objects are instantiated from classes in PHP

In PHP, objects are created (instantiated) from classes using the new keyword.
A class acts as a blueprint, and an object is a real instance of that class.
When an object is instantiated, memory is allocated for it, and it can access the properties and methods defined inside the class.

The process of object instantiation allows us to use the features and functionalities of a class in a real program.

Syntax:
$objectName = new ClassName();




PracticalExercise:
***) Instantiate multiple objects of the "Car" class and demonstrate how to access their
 properties and methods.
->
	
<?php

class car
{
    public $make;
    public $model;
    public $year;
    function __construct($make,$model,$year)
    {
            $this->make =$make;
            $this->model =$model;
            $this->year =$year;
    }
    function display()
    {
         echo "Car Make :". $this->make."<br>";
           echo" Car Model :". $this->model."<br>";
             echo"Car Year :". $this->year."<br>";
    }
 }
$ob = new car('toyota','camry',2020);
$ob->display();

$ob1 =new car('thar','mahendra',2017);
$ob1->display();


$ob2 =new car('punch','tata',2017);
$ob1->display();




**Extends**

1)Explain the concept of inheritance in OOP and how itis implemented in PHP ?

=> Concept of Inheritance in OOP (Theory)

Inheritance is one of the main pillars of Object-Oriented Programming (OOP).
It allows a class (called a child class or subclass) to inherit properties and methods from another class (called a parent class or base class).

=> Definition:

Inheritance is the mechanism by which one class can use the properties and methods of another class.

=> Purpose of Inheritance:

Code Reusability: Avoids rewriting the same code in multiple classes.

Extensibility: Allows you to extend or modify the functionality of existing classes.

Organization: Helps create a clear class hierarchy.

Maintainability: Easier to manage and update code

type of Inheritance 

1)single  Inheritance 
2)hirarchical Inheritance 
3)multi-level-Inheritance 
4)multiple Inheritance (not supported)
5)Hybrid Inheritance




1) Create a "Vehicle"class and extend it with a"Car"class.Include properties and methods in
 classes,demonstrating inherited behavior.
=>

<?php

class Vehicle
{
   public $brand;
     public $model;

     function __construct($brand,$model)
     {
        $this->brand =$brand;
        $this->model =$model;

     }
     function display()
     {
        echo "Brand    :".$this->brand."<br>";
        echo "Model  :".$this->model."<br>";
     }
}
class car extends Vehicle
{
     public $year;

     function __construct($brand,$model,$year)
     {
        parent::__construct($brand,$model);
        $this->year =$year;
     }
     
     function getdisplay()
     {
         echo "year   :".$this->year;
     }
}

$obj = new car('BMW','3 series',2020);
$obj->display();
$obj->getdisplay();
?>



***Overloading***

1)Discuss method overloading and how itis implemented in PHP ?
=>Definition:

Method Overloading is a concept in Object-Oriented Programming (OOP) where multiple methods in the same class share the same name but differ in the number or type of parameters.

It allows a class to perform different tasks using the same method name.




1) Create a class that demonstrates method overloading by defining multiple methods with the
 same name but different parameters.

=>

<?php

class A
{
    
    function display($a,$b)
    {
        echo "Addition   :".($a + $b);
    }
}
class B extends A
{

    function display($a,$b,$c =null)
    {
    if($c ==   null)
    {
        parent::display($a,$b);

    }
    else
    {
        
        echo "multiplication   :".($a*$b*$c);   
    
    }
}
   
}

$b1 = new B();
// $b1->display(2,3,4);
$b1->display(10,20,30);
echo "<br>";
$b1->display(20,30);


?>



**AbstractionInterface**
1)Explain the concept of abstraction and the use of interfaces in PHP
=>
What is Abstraction?

Abstraction is one of the main pillars of Object-Oriented Programming (OOP).
It means hiding the complex implementation details and showing only the essential features of an object.

In simple terms:
You only show what an object does, not how it does it.

=> Purpose of Abstraction:

To simplify complex systems by breaking them into smaller, understandable parts.

To focus on essential operations rather than internal logic.

To achieve loose coupling between components (less dependency).

To enforce a common structure for multiple classes.

=> How Abstraction is Implemented in PHP:

In PHP, abstraction can be achieved in two ways:

Using Abstract Classes

Using Interfaces



1) Define an interface named Vehicle Interface with methods like start(),stop(),and
 implement this interface in multiple classes.
=>
<?php

interface VehicleInterface {
    public function start();
    public function stop();
}

// Class 1: Car implements VehicleInterface
class Car implements VehicleInterface {
    public function start() {
        echo "Car engine started.<br>";
    }

    public function stop() {
        echo "Car engine stopped.<br>";
    }
}


class Bike implements VehicleInterface {
    public function start() {
        echo "Bike engine started.<br>";
    }

    public function stop() {
        echo "Bike engine stopped.<br>";
    }
}


$car = new Car();
$bike = new Bike();

$car->start();
$car->stop();

$bike->start();
$bike->stop();
?>



***)  What is a constructor in PHP ? Discuss its purpose and how itis used ?.
=>
A constructor in PHP is a special method that is automatically called when an object of a class is created.

It is used to initialize object properties or perform setup tasks when the object starts its life.


function __construct(parameters) {
   
}

=>Purpose of a Constructor:

To initialize object properties when the object is created.

To set default values or pass values when creating objects.

To execute code automatically without calling the method manually.

To simplify object initialization and make the code cleaner.


1)Create a class with a constructor that initializes properties when an objectis created ?
=>

<?php
class argument
{

    public function __construct($name,$email,$phone,$pincode){

        echo " name is:  ".$name.'<br>';
        echo " email is ".$email.'<br>';
        echo " phone number is:  ".$phone.'<br>';
        echo " pin code number is:   ".$pincode.'<br>';

    }


}

$ob =new argument("inzazmul","In@gmail.com",8238489638,363621);

?>


**Destructor**
1)Explain the role of a destructor in PHP and when itis called ?
=>
Definition:

A destructor in PHP is a special method that is automatically called when an object is destroyed or goes out of scope.

It is used to perform cleanup tasks, such as closing files, releasing memory, or disconnecting from a database.

function __destruct() {
    // cleanup code
}

1)Write a class that implements a destructor to perform clean up tasks when an object is
 destroyed.
=>
<?php

class emp
{
    public $a;
    public $b;

    function __construct($a,$b)
    {
       $this->a=$a;
         $this->b=$b;
    }
   function __destruct()
   {
       echo "object is destroy";
   }
}

$ob= new emp(12,30);

?>



**MagicMethods**
**) Define  magic methods in PHP.Discuss commonly used magic methods like__get(),
 __set(),and__construct().
=>
Definition:

Magic methods in PHP are special built-in methods that begin with a double underscore (__).
They are automatically triggered by certain actions in a class — for example, when creating an object, accessing an undefined property, or calling a non-existent method.

=> In simple terms:
Magic methods allow you to customize how objects behave in specific situations.

purpose of Magic Methods:

To control or override default behavior of PHP classes and objects.

To handle dynamic operations such as reading/writing inaccessible properties.

To initialize or clean up objects automatically (using __construct() and __destruct()).





1) Create a class that uses magic methods to handle property access and modification
 dynamically.

<?php
class Student {
    private $data = [];

  
    public function __set($name, $value) {
        $this->data[$name] = $value;
    }

   
    public function __get($name) {
        return $this->data[$name];
    }
}


$stu = new Student();


$stu->name = "inzamul";
$stu->age = 20;

echo "Name: " . $stu->name . "<br>";
echo "Age: " . $stu->age;
?>


*** Scope Resolution**
1)  Explain the  scope resolution operator(::) and its use in PHP.
=>Definition:

The Scope Resolution Operator (::) in PHP is a special symbol used to access class 
constants, static properties, and static methods, as well as refer to parent or current
 class members from within a class.


syntax:

ClassName::memberName


1)Create a class with static properties and methods ,and demonstrate their access using the
 scope resolution operator. ?
=>
<?php
class Math {
    public static $number = 10;   // static property

    public static function square() {   // static method
        return self::$number * self::$number;  // access using self::
    }
}


echo "Number: " . Math::$number . "<br>";


echo "Square: " . Math::square();
?>




**traits**
1)  Define traits in PHP and their purpose in code reuse.
=>
What is a Trait in PHP?

A Trait in PHP is a mechanism for code reuse that allows you to include methods in multiple classes without using inheritance.
=>multiple inheritance support in traits

Purpose of Traits:

To reuse code across multiple classes.

To avoid duplication of methods.

To solve the limitation of single inheritance (since PHP classes can only extend one class).

To keep code modular and easier to maintain


1)  Create two traits and use the min a class to demonstrate how to include multiple behaviors.
=>

<?php
trait a 
{
    function first()
    {
    echo "this is a first trait";
    }
}


trait b 
{
    function second()
    {
    echo "this is a second traits";
    }
}
class emp 
{
    use a,b;
}
$ob= new emp();
$ob->first();
echo "<br>";
$ob->second();
?>




***Visibility**
1)  Discuss the visibility of properties and methods in PHP (public,private,protected).
=>
Define Public
=> public access Visibility is any where access.
=>by default is variable in public

2)Define Private
=> A private property or method can only be accessed within the same class where it is defined.
It cannot be accessed:
=>From outside the class and From child (inherited) classe

3)Definition: protected
In PHP, the protected access modifier means that a property or method can be accessed only:
Within the same class, andBy classes that inherit (extend) it.
It cannot be accessed directly from outside the class.



1) Write a class thats hows examples of each visibility type and how they restrict access to
 properties and methods.
=>
<?php

class A {
    public $public="  i am a public";

    protected $protected="I am a protected";

    private $privete=" I am a privare";

    function display()
    {
         echo $this->public.'<br>';
       echo  $this->protected.'<br>';
         echo $this->private."<br>";
    }

}

$a = new A();
$a->display();

?>




**Final Keyword **
1) Discuss the purpose of the final keyword in PHP and how it affects classes and methods
=>
Definition:

In PHP, the final keyword is used to restrict inheritance or method overriding.
It can be applied to both classes and methods.

Purpose:

The main purpose of the final keyword is to protect code from being modified in subclasses.
It ensures that:

A final class cannot be inherited.

A final method cannot be overridden in a child class.


1)Create a class marked as final and attempt to extend it to show the restriction
=>
<?php

final class Vehicle {
    public function display() {
        echo "This is a Vehicle class.<br>";
    }
}


class Car extends Vehicle {  
    public function show() {
        echo "This is a Car class.<br>";
    }
}

// Create an object
$obj = new Car();
$obj->display();
?>

final class is cannot inherit in another class.



**FileHandling
1)Discuss filehandling in PHP,including opening,reading,writing,andclosing files
=>ile handling in PHP allows you to create, open, read, write, and close files stored on the server.
It is commonly used for tasks like saving data, reading configuration files, or logging user activity.

PHP provides built-in functions to perform all these operations safely and efficiently.

fopen() => Opens a file for reading or writing
fread()  => Reads data from a file
fwrite()  =>	Writes data to a file
fclose() => Closes an open file

1)  Create a script that reads from a text file and displays its content on a webpage.
=>
<?php

$filename = "example.txt";


if (file_exists($filename)) {

    $file = fopen($filename, "r");

    echo "<h3>File Content:</h3>";

  
    }


    fclose($file);
} else {
    echo "File not found!";
}
?>

**MVCArchitecture**
THEORY EXERCISE
1)Discuss the Model-View-Controller (MVC) architecture and its advantages in web
 development.
=> MVC (Model–View–Controller) is a software architectural pattern used to separate an application into three main components:

1.Model

2.View

3.Controller

It helps in organizing code cleanly, making it reusable, scalable, and easier to maintain.

1. Model

The Model represents the data and business logic of the application.

Responsibilities:

Interact with the database

Handle CRUD operations (Create, Read, Update, Delete)

Apply business rules / validations

Return data to the controller

2.View

The View is the presentation layer.

Responsibilities:

Display the user interface

Show data received from the controller

Should NOT contain business logic

3.Controller

The Controller handles the application flow.

Responsibilities:

Receive user requests

Call appropriate Model functions

Pass data to the View

Control navigation



* Practical*
1)Createa simple MVC application that demonstrates the separation of concerns by
 implementinga basic "User"module with a model,view,and controller
=>
index.php 

<?php
include ("class/dbcontroller.php");
require_once ("class/student.php");
$db_hndle = new database();

$action = "";
if (! empty($_GET["action"])) 
    {
    $action = $_GET["action"];
}
switch ($action) {
   
    case "student-add":
        if (isset($_POST['add'])) {
            $name = $_POST['name'];
            $roll_number = $_POST['roll_number'];
            $dob = "";
            if ($_POST["dob"]) {
                $dob_timestamp = strtotime($_POST["dob"]);
                $dob = date("Y-m-d", $dob_timestamp);
            }
            $class = $_POST['class'];
            
            $student = new student();
            $insertId = $student->add($name, $roll_number, $dob, $class);
            
            if (empty($insertId)) {
                $response = array(
                    "message" => "Problem in Adding New Record",
                    "type" => "error"
                );
            } else {
                header("Location: index.php");
            }
        }
        require_once "web/student-add.php";
        break;
    
    
    default:
        $student = new student();
         require_once "web/student.php";
        break;
}
?>

class/dbcontroller.php

<?php
class database
{
    private $con;
    private $host="localhost";
    private $user="root";
    private $pass="";
    private $db="mvc1";

    function __construct()
    {
        $this->con=$this->connectdb();
        
    }

    function connectdb()
    {
        $con=new mysqli($this->host,$this->user,$this->pass,$this->db);
        return $con;
    }

    
    function bindQueryParams($sql,$paramtype,$paramvalue){

        $param_value_reference []= & $paramtype;
        for ($i=0; $i <count($paramvalue) ; $i++) { 
            $param_value_reference[]= & $paramvalue[$i];           
        }
         call_user_func_array(array($sql,  'bind_param' ), $param_value_reference);
        
    }


    function insert($query,$paramtype,$paramvalue)
    {
        $sql=$this->con->prepare($query);
        $this->bindQueryParams($sql,$paramtype,$paramvalue);
        $sql->execute();
        $inserid=$sql->inser_id;
        return $inserid;
    }
}
?>
class/student.php

<?php
class student 
{
    private $con;
    function  __construct()
    {
        $this->con=new database();
    }

    function add($name,$roll_number,$dob,$class)
    {

        $query="INSERT INTO tbl_student (name,roll_number,dob,class)VALUES(?,?,?,?)";
        $paramtype='siss';
        $paramvalue=array($name,$roll_number,$dob,$class);

         $insertid=$this->con->insert($query,$paramtype,$paramvalue);
         return $insertid ;
    }
}
?>

web/student.php

<?php require_once "web/header.php"; ?>
    <div style="text-align: right; margin: 20px 0px 10px;">
        <a id="btnAddAction" href="index.php?action=student-add"><img src="web/image/icon-add.png" />Add Student</a>
    </div>
  
web/header.php

<html>
<head>
<title>How to Create PHP Crud using OOPS and MySQLi</title>
<link href="web/css/style.css" type="text/css" rel="stylesheet" />
</head>
<body>
    <h2>How to Create PHP Crud using OOPS and MySQLi</h2>
    <div>
        <ul class="menu-list">
            <li><a href="index.php">Student</a></li>
            <li><a href="index.php?action=attendance">Attendance</a></li>
        </ul>
    </div>


web/student-add.php

<?php require_once "web/header.php"; ?>

<form name="frmAdd" method="post" action="" id="frmAdd"
    onSubmit="return validate();">
    <div id="mail-status"></div>
    <div>
        <label style="padding-top: 20px;">Name</label> <span
            id="name-info" class="info"></span><br /> <input type="text"
            name="name" id="name" class="demoInputBox">
    </div>
    <div>
        <label>Roll Number</label> <span id="roll-number-info"
            class="info"></span><br /> <input type="text"
            name="roll_number" id="roll_number" class="demoInputBox">
    </div>
    <div>
        <label>Date of Birth</label> <span id="dob-info" class="info"></span><br />
        <input type="date" name="dob" id="dob" class="demoInputBox">
    </div>
    <div>
        <label>Class</label> <span id="class-info" class="info"></span><br />
        <input type="text" name="class" id="class" class="demoInputBox">
    </div>
    <div>
        <input type="submit" name="add" id="btnSubmit" value="Add" />
    </div>
    </div>
</form>
<script src="https://code.jquery.com/jquery-2.1.1.min.js"
    type="text/javascript"></script>
<script>
function validate() {
    var valid = true;   
    $(".demoInputBox").css('background-color','');
    $(".info").html('');
    
    if(!$("#name").val()) {
        $("#name-info").html("(required)");
        $("#name").css('background-color','#FFFFDF');
        valid = false;
    }
    if(!$("#roll_number").val()) {
        $("#roll-number-info").html("(required)");
        $("#roll_number").css('background-color','#FFFFDF');
        valid = false;
    }
    if(!$("#dob").val()) {
        $("#dob-info").html("(required)");
        $("#dob").css('background-color','#FFFFDF');
        valid = false;
    }
    if(!$("#class").val()) {
        $("#class-info").html("(required)");
        $("#class").css('background-color','#FFFFDF');
        valid = false;
    }   
    return valid;
}
</script>
</body>
</html>


** Connection with MySQL  Database
THEORY EXERCISE
1)  Explain how to connect PHP to a MySQL database using mysqli or PDO.
=>

<?php
$host = "localhost";
$username = "root";
$password = "";
$database = "db_1";


$conn = new mysqli($host, $username, $password, $database);

if ($conn->connect_error) {
    die("Database Connection Failed: " . $conn->connect_error);
}

?>


 Practical Example: Implementation of all the OOPs Concepts

PracticalExercise
** Develop a mini project(e.g.,aLibraryManagementSystem)thatutilizesallOOPconcepts
 likeclasses,inheritance,interfaces,magicmethods,etc.
=>
index.php

<?php
require_once "app/controllers/BookController.php";

$controller = new BookController();
$action = isset($_GET['action']) ? $_GET['action'] : 'index';

if ($action == "store") {
    $controller->store();
} else {
    $controller->index();
}
?>

config/ database.php

<?php
class Database
{
    private $host="localhost";
    private $user="root";
    private $pass="";
    private $db="db_12";

    public function connect() 
    {
        $con = new mysqli($this->host,$this->user,$this->pass,$this->db);
        return $con;
    }
}
?>

app/controllers/BookController.php

<?php
require_once __DIR__."/../models/Book.php";

class BookController
{
    private $book;

    public function __construct()
    {
        $this->book = new Book();
    }

    public function index()
    {
        $books = $this->book->getAll();
        require __DIR__."/../views/books.php";
    }

    public function store()
    {
        $this->book->add($_POST);
        header("Location: index.php");
    }
}

?>
models/Book.php
<?php
require_once "Model.php";
require_once "CrudInterface.php";

class Book extends Model implements CrudInterface
{
    private $table = "books";

    public function __get($name)
    {
        return $this->$name;
    }

    public function add($data)
    {
        $name = $data['name'];
        $author = $data['author'];

        $sql = "INSERT INTO $this->table (name,author) VALUES ('$name','$author')";
        return $this->db->query($sql);
    }

    public function getAll()
    {
        return $this->db->query("SELECT * FROM $this->table");
    }
}
?>

models/Crudinterface.php
<?php
interface CrudInterface
{
    public function add($data);
    public function getAll();
}
?>

models/Model.php
<?php
require_once __DIR__."/../../config/Database.php";

abstract class Model
{
    protected $db;

    public function __construct()
    {
        $database = new Database();
        $this->db = $database->connect();
    }
}
?>
views/books.php
<!DOCTYPE html>
<html>
<head>
    <title>Library Management</title>
</head>
<body>

<h2>Add Book</h2>

<form method="post" action="index.php?action=store">
    <input type="text" name="name" placeholder="Book Name" required>
    <input type="text" name="author" placeholder="Author" required>
    <button type="submit">Add</button>
</form>

<hr>

<h2>Book List</h2>

<table border="1">
<tr>
    <th>ID</th>
    <th>Name</th>
    <th>Author</th>
</tr>

<?php while($row = $books->fetch_assoc()): ?>
<tr>
    <td><?= $row['id'] ?></td>
    <td><?= $row['name'] ?></td>
    <td><?= $row['author'] ?></td>
</tr>
<?php endwhile; ?>

</table>

</body>
</html>



***Connection  with MySQLDatabas***
THEORYEXERCISE:
  Explain how to connect PHP to a MySQL database using my sqli or PDO
=>
MySQLi stands for MySQL Improved.
It is an extension that allows PHP to communicate with a MySQL database.

It supports two types of connections:

Procedural style

Object-oriented style

** Steps to connect using MySQLi:

Define database credentials:

Host (usually localhost)

Username

Password

Database name

Create a connection using mysqli_connect() (procedural)
OR new mysqli() (OOP)

Check if the connection is successful

Handle errors if connection fails

 Key Features of MySQLi:

Supports procedural and OOP syntax

Faster and simpler for MySQL-only projects

Supports prepared statements

Works only with MySQL database

** Advantages of MySQLi:**

Easy to use

Good performance

Supports security (prepared statements)

**Practical Exercise***
** Writea script to establish a database connection and handle any errors during the
 connectionprocess
=>
<?php

$host = "localhost";
$user = "root";
$pass = "";
$db   = "testdb";

$conn = new mysqli($host, $user, $pass, $db);

if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}

echo "Database connected successfully!";
?>

***SQL Injection**
**THEORY EXERCISE:
  Define SQL injection and its implications on security.
=>QL Injection is a type of cyber attack where an attacker inserts or “injects” malicious SQL code into an application's input fields to manipulate, alter, or access a database.

It occurs when user inputs are not properly validated or sanitized, allowing attackers to change the SQL query executed by the server.

***Practical Exercise**
**Demonstrate a vulnerable SQL query and then show how to preventSQL injection  using
 prepared statements
=>
<?php

$username = $_POST['username'];
$password = $_POST['password'];

$conn = mysqli_connect("localhost", "root", "", "testdb");


$sql = "SELECT * FROM users WHERE username = ? AND password = ?";

$stmt = mysqli_prepare($conn, $sql);


mysqli_stmt_bind_param($stmt, "ss", $username, $password);


mysqli_stmt_execute($stmt);


$result = mysqli_stmt_get_result($stmt);

if(mysqli_num_rows($result) > 0){
    echo "Login successful!";
} else {
    echo "Invalid credentials!";
}
?>


 Practical:ExceptionHandlingwithTry-CatchforDatabaseConnectionand
 Queries
=>
<?php

$host = "localhost";
$db   = "testdb";
$user = "root";
$pass = "";

try {

    $dsn = "mysql:host=$host;dbname=$db";
    $conn = new PDO($dsn, $user, $pass);


    $conn->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);

    echo "Database connected successfully! <br>";

    $sql = "SELECT * FROM users";
    $result = $conn->query($sql);

    echo "Query executed successfully! <br>";

    foreach ($result as $row) {
        echo "User: " . $row['username'] . "<br>";
    }

} catch (PDOException $e) {
    echo "Error: " . $e->getMessage();
}

?>

   
    if($valid){
        $success = "Registration Successful!";
    }
}
?>

 **Session and Cookies
**THEORY EXERCISE
1) Explain the differences between sessions and cookies in PHP ?
=>
A cookie is a small piece of data stored on the client’s browser.
PHP can send cookies to the browser, which stores them and sends them back on every request.

Key Characteristics:

Data stored on the client side

Can store small amounts of data (generally up to 4 KB)

Data can persist even after the browser is closed (if expiry time is set)

Not secure by default

**Practical Exercise**
1) Writea script to create a session and store user data,and then retrieve it on a different page.
 Also,demonstrate how to set and retrieve a cookie
=>
page1.php

<?php
session_start(); 


$_SESSION['username'] = "John Doe";
$_SESSION['email'] = "john@example.com";


setcookie("user_city", "Mumbai", time() + 3600, "/");

echo "Session data and cookie set successfully!";
?>

<br><br>
<a href="page2.php">Go to Page 2</a>

page2.php
=>
<?php
session_start(); // Must start session on every page using session

echo "<h3>Session Data</h3>";

if(isset($_SESSION['username'])) {
    echo "Username: " . $_SESSION['username'] . "<br>";
    echo "Email: " . $_SESSION['email'] . "<br>";
} else {
    echo "No session data found!";
}

echo "<h3>Cookie Data</h3>";

if(isset($_COOKIE['user_city'])) {
    echo "City: " . $_COOKIE['user_city'];
} else {
    echo "Cookie not found!";
}
?>

**FileUpload**
**THEORY EXERCISE**
** Discuss fileupload functionality in PHP and its security implications
=>
What is File Upload in PHP?

File upload functionality allows users to upload files from their local system to a server through an HTML form.
PHP handles uploaded files using the $_FILES superglobal.




****PracticalExercise:**
1 . Create a fileupload form that allows users to upload files and handle the uploaded files
 safely on the server ?
=>
upload.php

<!DOCTYPE html>
<html>
<head>
    <title>Simple File Upload</title>
</head>
<body>

<h2>Upload File</h2>

<form action="upload_action.php" method="post" enctype="multipart/form-data">
    <input type="file" name="myfile" required>
    <br><br>
    <button type="submit" name="upload">Upload</button>
</form>

</body>
</html>

upload_action.php

<?php

if (isset($_POST['upload'])) {

    $uploadDir = "uploads/"; // folder already exists

    $fileName = basename($_FILES['myfile']['name']);
    $fileTmp  = $_FILES['myfile']['tmp_name'];

    // Create unique name to avoid overwrite
    $newName = time() . "_" . $fileName;

    $targetPath = $uploadDir . $newName;

    if (move_uploaded_file($fileTmp, $targetPath)) {
        echo "File uploaded successfully <br>";
        echo "Saved as: " . $newName;
    } else {
        echo "File upload failed";
    }
}
?>

***SendMail While Registration **
***PracticalExercise**
1) Extend the registration form to send a confirmation email upon successful registration ?
=>
register.php

<!DOCTYPE html>
<html>
<head>
    <title>User Registration</title>
</head>
<body>

<h2>Register</h2>

<form action="register_action.php" method="post">
    <input type="text" name="name" placeholder="Full Name" required><br><br>
    <input type="email" name="email" placeholder="Email" required><br><br>
    <input type="password" name="password" placeholder="Password" required><br><br>
    <button type="submit" name="register">Register</button>
</form>

</body>
</html>

register_action.php

<?php
// Database connection
$conn = new mysqli("localhost", "root", "", "test12");

if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}

if(isset($_POST['register'])) {

    $name = $conn->real_escape_string($_POST['name']);
    $email = $conn->real_escape_string($_POST['email']);
    $password = password_hash($_POST['password'], PASSWORD_DEFAULT);

    // Insert user into database
    $sql = "INSERT INTO users (name, email, password) VALUES ('$name', '$email', '$password')";

    if($conn->query($sql)) {
        // Send confirmation email
        $to = $email;
        $subject = "Registration Successful";
        $message = "Hello $name,\n\nThank you for registering on our website.\n\nRegards,\nWebsite Team";
        $headers = "From: no-reply@example.com";

        if(mail($to, $subject, $message, $headers)) {
            echo "Registration successful! A confirmation email has been sent to $email.";
        } else {
            echo "Registration successful, but email could not be sent.";
        }
    } else {
        echo "Error: " . $conn->error;
    }
}
?>

**server-Side Validation while Registration using Regular Expressions**
**Practical Exercise:**
1) Write a registration form that validates user input(e.g.,email,password)using regular
expressions before submission.
=>
register.php

<!DOCTYPE html>
<html>
<head>
    <title>Registration Form</title>
    <script>
        function validateForm() {
            var email = document.forms["regForm"]["email"].value;
            var password = document.forms["regForm"]["password"].value;

            // Email regex
            var emailPattern = /^[a-zA-Z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,4}$/;
            if(!emailPattern.test(email)) {
                alert("Invalid email format");
                return false;
            }

            // Password regex (minimum 6 chars, 1 uppercase, 1 lowercase, 1 number)
            var passwordPattern = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{6,}$/;
            if(!passwordPattern.test(password)) {
                alert("Password must be at least 6 characters long, with at least 1 uppercase, 1 lowercase, and 1 number.");
                return false;
            }

            return true;
        }
    </script>
</head>
<body>

<h2>User Registration</h2>

<form name="regForm" action="register_action.php" method="post" onsubmit="return validateForm();">
    <input type="text" name="name" placeholder="Full Name" required><br><br>
    <input type="email" name="email" placeholder="Email" required><br><br>
    <input type="password" name="password" placeholder="Password" required><br><br>
    <button type="submit" name="register">Register</button>
</form>

</body>
</html>

register_action.php

<?php
// Database connection
$conn = new mysqli("localhost", "root", "", "test12");
if($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}

if(isset($_POST['register'])) {
    $name = $conn->real_escape_string($_POST['name']);
    $email = $conn->real_escape_string($_POST['email']);
    $password = $_POST['password'];

    // Server-side email validation
    if(!preg_match("/^[a-zA-Z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,4}$/", $email)) {
        die("Invalid email format");
    }

    // Server-side password validation
    if(!preg_match("/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{6,}$/", $password)) {
        die("Password must be at least 6 characters long, with at least 1 uppercase, 1 lowercase, and 1 number.");
    }

    // Hash password
    $hashedPassword = password_hash($password, PASSWORD_DEFAULT);

    // Insert into database
    $sql = "INSERT INTO users (name, email, password) VALUES ('$name', '$email', '$hashedPassword')";
    if($conn->query($sql)) {
        echo "Registration successful!";
    } else {
        echo "Error: " . $conn->error;
    }
}
?>


**PHP with MVC Architecture**
**Practical Exercise**
1) Implement a CRUD application(Create,Read,Update,Delete)using the MVC architecture to
manage user data ?.
=>
config/database.php
<?php
class Database
{
    private $host = "localhost";
    private $user = "root";
    private $pass = "";
    private $db = "test12";

    public function connect() {
        $con = new mysqli($this->host, $this->user, $this->pass, $this->db);
        if($con->connect_error) {
            die("Connection failed: " . $con->connect_error);
        }
        return $con;
    }
}
?>

app/models/Model.php

<?php
require_once "config/Database.php";

abstract class Model
{
    protected $db;

    public function __construct() {
        $database = new Database();
        $this->db = $database->connect();
    }
}
?>

app/models/User.php

<?php
require_once "Model.php";

class User extends Model
{
    private $table = "users";

    public function getAll() {
        return $this->db->query("SELECT * FROM $this->table");
    }

    public function getById($id) {
        $id = intval($id);
        return $this->db->query("SELECT * FROM $this->table WHERE id=$id")->fetch_assoc();
    }

    public function create($data) {
        $name = $this->db->real_escape_string($data['name']);
        $email = $this->db->real_escape_string($data['email']);
        $password = password_hash($data['password'], PASSWORD_DEFAULT);
        return $this->db->query("INSERT INTO $this->table (name,email,password) VALUES ('$name','$email','$password')");
    }

    public function update($id, $data) {
        $id = intval($id);
        $name = $this->db->real_escape_string($data['name']);
        $email = $this->db->real_escape_string($data['email']);
        $password = password_hash($data['password'], PASSWORD_DEFAULT);
        return $this->db->query("UPDATE $this->table SET name='$name', email='$email', password='$password' WHERE id=$id");
    }

    public function delete($id) {
        $id = intval($id);
        return $this->db->query("DELETE FROM $this->table WHERE id=$id");
    }
}
?>

app/controllers/UserController.php
<?php
require_once "app/models/User.php";

class UserController
{
    private $user;

    public function __construct() {
        $this->user = new User();
    }

    public function index() {
        $users = $this->user->getAll();
        require "app/views/users.php";
    }

    public function create() {
        if(isset($_POST['submit'])) {
            $this->user->create($_POST);
            header("Location: index.php");
        }
        require "app/views/edit_user.php"; // same form used for create/edit
    }

    public function edit($id) {
        if(isset($_POST['submit'])) {
            $this->user->update($id, $_POST);
            header("Location: index.php");
        }
        $userData = $this->user->getById($id);
        require "app/views/edit_user.php";
    }

    public function delete($id) {
        $this->user->delete($id);
        header("Location: index.php");
    }
}
?>
app/views/users.php
<!DOCTYPE html>
<html>
<head>
    <title>User Management</title>
</head>
<body>

<h2>User List</h2>
<a href="index.php?action=create">Add New User</a>
<table border="1">
<tr>
    <th>ID</th>
    <th>Name</th>
    <th>Email</th>
    <th>Action</th>
</tr>

<?php while($row = $users->fetch_assoc()): ?>
<tr>
    <td><?= $row['id'] ?></td>
    <td><?= $row['name'] ?></td>
    <td><?= $row['email'] ?></td>
    <td>
        <a href="index.php?action=edit&id=<?= $row['id'] ?>">Edit</a> | 
        <a href="index.php?action=delete&id=<?= $row['id'] ?>" onclick="return confirm('Delete this user?')">Delete</a>
    </td>
</tr>
<?php endwhile; ?>

</table>

</body>
</html>


app/views/edit_user.php
<!DOCTYPE html>
<html>
<head>
    <title><?= isset($userData) ? "Edit User" : "Add User" ?></title>
</head>
<body>

<h2><?= isset($userData) ? "Edit User" : "Add User" ?></h2>

<form method="post">
    <input type="text" name="name" placeholder="Full Name" value="<?= $userData['name'] ?? '' ?>" required><br><br>
    <input type="email" name="email" placeholder="Email" value="<?= $userData['email'] ?? '' ?>" required><br><br>
    <input type="password" name="password" placeholder="Password" required><br><br>
    <button type="submit" name="submit"><?= isset($userData) ? "Update" : "Create" ?></button>
</form>

</body>
</html>


index.php

<?php
require_once "app/controllers/UserController.php";

$controller = new UserController();

$action = $_GET['action'] ?? 'index';
$id = $_GET['id'] ?? null;

switch($action) {
    case 'create':
        $controller->create();
        break;
    case 'edit':
        $controller->edit($id);
        break;
    case 'delete':
        $controller->delete($id);
        break;
    default:
        $controller->index();
        break;
}
?>


***Insert,Update,DeleteMVC***
Practical Exercise:
1)Extend the CRUD applicationto include functionalities for inserting,updating,and deleting
user records,ensuring properseparation of concerns in the MVC structure.
=>
config/Database.php
<?php
class Database
{
    private $host = "localhost";
    private $user = "root";
    private $pass = "";
    private $db = "test12";

    public function connect() {
        $con = new mysqli($this->host, $this->user, $this->pass, $this->db);
        if($con->connect_error) {
            die("Connection failed: " . $con->connect_error);
        }
        return $con;
    }
}
?>

app/models/Model.php
<?php
require_once "config/Database.php";

abstract class Model
{
    protected $db;

    public function __construct() {
        $database = new Database();
        $this->db = $database->connect();
    }
}
?>
app/models/User.php
<?php
require_once "Model.php";

class User extends Model
{
    private $table = "users";

    public function getAll() {
        return $this->db->query("SELECT * FROM $this->table");
    }

    public function getById($id) {
        $id = intval($id);
        return $this->db->query("SELECT * FROM $this->table WHERE id=$id")->fetch_assoc();
    }

    public function create($data) {
        $name = $this->db->real_escape_string($data['name']);
        $email = $this->db->real_escape_string($data['email']);
        $password = password_hash($data['password'], PASSWORD_DEFAULT);
        return $this->db->query("INSERT INTO $this->table (name,email,password) VALUES ('$name','$email','$password')");
    }

    public function update($id, $data) {
        $id = intval($id);
        $name = $this->db->real_escape_string($data['name']);
        $email = $this->db->real_escape_string($data['email']);
        $password = password_hash($data['password'], PASSWORD_DEFAULT);
        return $this->db->query("UPDATE $this->table SET name='$name', email='$email', password='$password' WHERE id=$id");
    }

    public function delete($id) {
        $id = intval($id);
        return $this->db->query("DELETE FROM $this->table WHERE id=$id");
    }
}
?>

app/controllers/UserController.php
<?php
require_once "app/models/User.php";

class UserController
{
    private $user;

    public function __construct() {
        $this->user = new User();
    }

    // Display all users
    public function index() {
        $users = $this->user->getAll();
        require "app/views/users.php";
    }

    // Show create form and insert user
    public function create() {
        if(isset($_POST['submit'])) {
            $this->user->create($_POST);
            header("Location: index.php");
        }
        require "app/views/user_form.php";
    }

    // Show edit form and update user
    public function edit($id) {
        $userData = $this->user->getById($id);
        if(isset($_POST['submit'])) {
            $this->user->update($id, $_POST);
            header("Location: index.php");
        }
        require "app/views/user_form.php";
    }

    // Delete user
    public function delete($id) {
        $this->user->delete($id);
        header("Location: index.php");
    }
}
?>

app/views/users.php
<!DOCTYPE html>
<html>
<head>
    <title>User Management</title>
</head>
<body>

<h2>User List</h2>
<a href="index.php?action=create">Add New User</a>
<table border="1">
<tr>
    <th>ID</th>
    <th>Name</th>
    <th>Email</th>
    <th>Action</th>
</tr>

<?php while($row = $users->fetch_assoc()): ?>
<tr>
    <td><?= $row['id'] ?></td>
    <td><?= $row['name'] ?></td>
    <td><?= $row['email'] ?></td>
    <td>
        <a href="index.php?action=edit&id=<?= $row['id'] ?>">Edit</a> | 
        <a href="index.php?action=delete&id=<?= $row['id'] ?>" onclick="return confirm('Delete this user?')">Delete</a>
    </td>
</tr>
<?php endwhile; ?>

</table>

</body>
</html>
?>

app/views/user_form.php
<!DOCTYPE html>
<html>
<head>
    <title><?= isset($userData) ? "Edit User" : "Add User" ?></title>
</head>
<body>

<h2><?= isset($userData) ? "Edit User" : "Add User" ?></h2>

<form method="post">
    <input type="text" name="name" placeholder="Full Name" value="<?= $userData['name'] ?? '' ?>" required><br><br>
    <input type="email" name="email" placeholder="Email" value="<?= $userData['email'] ?? '' ?>" required><br><br>
    <input type="password" name="password" placeholder="Password" required><br><br>
    <button type="submit" name="submit"><?= isset($userData) ? "Update" : "Create" ?></button>
</form>

</body>
</html>
?>



index.php
<?php
require_once "app/controllers/UserController.php";

$controller = new UserController();

$action = $_GET['action'] ?? 'index';
$id = $_GET['id'] ?? null;

switch($action) {
    case 'create':
        $controller->create();
        break;
    case 'edit':
        $controller->edit($id);
        break;
    case 'delete':
        $controller->delete($id);
        break;
    default:
        $controller->index();
        break;
}
?>



***Handling Emails**
**THEORY EXERCISE:**
1)Explain how to send emails in PHP using the mail() function and the importance of
validating email addresses.
=>
mail(to, subject, message, headers, parameters);

to – Recipient email address (e.g., example@example.com).

subject – The subject of the email.

message – The main body of the email.

headers (optional) – Additional headers like From, Cc, Bcc, Reply-To. Example:



**Practical Exercise***
1)Write a PHP script to send a test email to a user using the mail() function ?.
=>

<?php

$to = "user@example.com";


$subject = "Test Email from PHP";

$message = "Hello,\n\nThis is a test email sent using PHP's mail() function.\n\nRegards,\nWebsite Team";


$headers = "From: no-reply@mywebsite.com\r\n";
$headers .= "Reply-To: no-reply@mywebsite.com\r\n";


if(mail($to, $subject, $message, $headers)) {
    echo "Test email sent successfully to $to";
} else {
    echo "Failed to send the test email.";
}
?>






















